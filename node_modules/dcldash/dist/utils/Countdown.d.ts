export declare class Dash_Countdown {
    private remaining;
    private timer;
    private seconds;
    private onCompleteMap;
    private onUpdateMap;
    private onSecondMap;
    constructor();
    setTimer(remaining: number): void;
    onUpdate(func: (remaining: number) => void): void;
    onSecond(func: (remaining: number) => void): void;
    onComplete(func: () => void): void;
    start(): void;
    stop(): void;
    onFrame(data: any, dt: number): void;
}
