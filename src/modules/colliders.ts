
   
   import { MeshVertices, MeshIndices } from "src/modules/buildmeshdata"
   
   
   
    // export function loadColliders(cannonWorld: CANNON.World): void {
    //     // Trimesh addition for ramp
    //     let rampShape = new CANNON.Trimesh(MeshVertices, MeshIndices)
    //     const rampBody = new CANNON.Body({
    //       position: new CANNON.Vec3(0, 0, 0),
    //     })
    //     rampBody.addShape(rampShape)
    //     cannonWorld.addBody(rampBody)
    //   }
  